id = "guidelines.intent.keep-documentation-aligned"
title = "Keep documentation aligned"
category = "documentation"
tags = ["cpp", "qt", "documentation", "synchronization", "behavior", "maintenance"]
relations = [
  { type = "specializes", target = "craftsperson/keep-documentation-aligned" },
]
status = "hypothesized"
confidence = 0.99
capability = "Users can trust docs to match current interfaces."
threat = "README and guides drift after API changes."
expectation = "Public behavior will evolve."
strategy = "Update docs with every public-interface change."
evidence = [{ type = "gate", description = "Changed interfaces have matching current examples.", required = true }]
tradeoff = "Documentation duplicates may drift anyway."
scope = { project = "guidelines", paths = ["plugins/cpp-qt-ecosystem/agents/cpp-qt-craftsperson.md"] }
sources = [{ type = "document", ref = "plugins/cpp-qt-ecosystem/agents/cpp-qt-craftsperson.md#quality-assurance-process", summary = "The source explicitly prescribes this judgment.", confidence = 1.0 }]
