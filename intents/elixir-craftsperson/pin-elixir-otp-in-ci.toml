id = "guidelines.intent.pin-elixir-otp-in-ci"
title = "Pin Elixir and OTP in CI"
category = "delivery"
tags = ["elixir", "testing", "formatting", "build-and-delivery", "compatibility", "runtime-versions"]
relations = [
  { type = "specializes", target = "craftsperson/preserve-supported-platform-baselines" },
]
status = "hypothesized"
confidence = 0.99
capability = "Builds, tests, and formatting remain reproducible across developer and CI environments."
threat = "Unpinned Elixir or OTP upgrades silently change formatter output, compiler behavior, or runtime semantics."
expectation = "Elixir and Erlang/OTP will continue releasing independently of application changes."
strategy = "Pin the exact Elixir and project-compatible OTP versions in CI."
evidence = [{ type = "manifest", description = "CI configuration declares exact Elixir and OTP versions that agree with the project toolchain.", required = true }]
tradeoff = "Pinned toolchains require deliberate maintenance and can delay useful runtime fixes."
scope = { project = "guidelines", paths = ["plugins/elixir-ecosystem/agents/elixir-craftsperson.md"] }
sources = [{ type = "document", ref = "plugins/elixir-ecosystem/agents/elixir-craftsperson.md#supported-versions-invariant", summary = "Requires exact Elixir and OTP pins in CI because formatter output can vary by version.", confidence = 1.0 }]
