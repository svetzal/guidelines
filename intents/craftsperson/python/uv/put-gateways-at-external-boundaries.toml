id = "guidelines.intent.put-gateways-at-external-boundaries"
title = "Put gateways at external boundaries"
category = "architecture"
tags = ["python", "uv", "testing", "dependencies", "persistence", "gateways"]
relations = [
  { type = "specializes", target = "craftsperson/put-gateways-at-external-boundaries" },
]
status = "hypothesized"
confidence = 0.99
capability = "Core behavior can substitute controlled adapters without depending directly on external libraries or services."
threat = "Filesystem, database, API, and git clients leak vendor details and nondeterminism into business logic."
expectation = "External integrations will change or be unavailable during isolated tests."
strategy = "Route every external interaction through a project-owned gateway class and substitute the gateway at test boundaries."
evidence = [{ type = "architecture_review", description = "Core modules depend on project-owned gateway contracts rather than concrete external clients.", required = true }]
tradeoff = "Gateway interfaces and mappings add ceremony for integrations that may be small and stable."
scope = { project = "guidelines", paths = ["plugins/python-ecosystem/agents/uv-python-craftsperson.md"] }
sources = [{ type = "document", ref = "plugins/python-ecosystem/agents/uv-python-craftsperson.md#functional-core-imperative-shell", summary = "Requires all external interactions to pass through mockable gateway classes.", confidence = 1.0 }]
