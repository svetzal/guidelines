id = "guidelines.intent.deterministic-resource-cleanup"
title = "Make resource cleanup deterministic on every exit path"
category = "reliability"
tags = ["python", "error-handling", "build-and-delivery", "resource-management", "cleanup", "ownership"]
relations = [
  { type = "specializes", target = "craftsperson/manage-resource-lifetimes-explicitly" },
]
status = "hypothesized"
confidence = 1.0
capability = "Files, connections, locks, and dynamic resource groups are reliably released after use."
threat = "Exceptions, early returns, and cancellation bypass manual cleanup and leak scarce resources."
expectation = "Resource-using code will gain more branches and failure paths over time."
strategy = "Manage resources with `with`, custom context managers, `contextlib.suppress`, and ExitStack where resource sets are dynamic."
evidence = [{ type = "test", description = "Success and exception-path tests verify each acquired resource is released, and review finds no manually paired acquire/release where a context manager applies.", required = true }]
tradeoff = "Context-manager wrappers add ceremony and can hide the exact point at which cleanup occurs."
scope = { project = "guidelines", paths = ["plugins/python-ecosystem/agents/python-craftsperson.md"] }
sources = [{ type = "document", ref = "plugins/python-ecosystem/agents/python-craftsperson.md#context-managers", summary = "The source requires context managers for all resources and names tools for custom, suppressed, and dynamic cleanup.", confidence = 1.0 }]
